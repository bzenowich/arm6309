{
  "name": "arm6309-hardware",
  "version": "0.0.0",
  "description": "Board layouts for the arm6309 machine",
  "scripts": {
    "check": "make check",
    "check:storage": "make -C storage check",
    "check:jedec": "make jedec",
    "check:cupl": "make cupl",
    "check:sync": "make -C archive/video sync",
    "check:scan": "make -C archive/video scan",
    "check:access": "make -C archive/video access",
    "check:regfile": "make -C archive/video regfile",
    "check:seqph": "make -C archive/video seqph",
    "check:seqctl": "make -C archive/video seqctl",
    "check:tile": "make -C archive/video tile",
    "check:cadence": "make -C archive/video cadence",
    "check:audio": "make -C audio check",
    "check:arom": "make -C audio check",
    "check:netlist": "make netlist",
    "check:place": "make place",
    "render:boards": "make render",
    "check:sim": "make sim-hand",
    "build": "make boards",
    "build:all": "make all",
    "dev": "tsci dev",
    "census": "make census",
    "census:audio": "make -C audio census",
    "probe:minimise": "bun run tools/gal/minimise.probe.ts",
    "probe:polarity": "bun run tools/gal/polarity.probe.ts",
    "gen:video": "make verilog",
    "check:video": "make sim",
    "check:sim:audio": "make -C audio sim",
    "check:sim:board": "make -C mainboard sim",
    "check:oracle": "make -C audio oracle",
    "check:vlen": "make -C archive/video vlen",
    "gen:pld": "make pld",
    "check:pxsel": "make -C archive/video pxsel",
    "check:docs": "make docs",
    "check:video3": "make -C video3 timing",
    "check:decode": "make decode",
    "rom": "make -C ../software/boot rom",
    "check:machine": "make machine",
    "check:modplay": "make -C audio modplay",
    "check:audio:all": "make -C audio bench",
    "check:reach": "make reach",
    "check:fold": "make fold",
    "check:pins": "make pins"
  },
  "type": "commonjs",
  "dependencies": {
    "@tscircuit/cli": "^0.1.2021"
  },
  "devDependencies": {
    "bun": "^1.4.2"
  },
  "overrides": {
    "circuit-json": "0.0.485"
  },
  "private": true
}
