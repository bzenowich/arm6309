# `io/serial/` — serial

**Not started.** Placeholder.

Open before anything else: the I/O window and the interrupt line — see
[`../README.md`](../README.md) and [`../../docs/machine.md`](../../docs/machine.md) §5.

Note that the CPU module already has a debug UART on `USART3` (`PC10`/`PC11`) — see
[`../../cpu/README.md`](../../cpu/README.md). That is a bring-up console for the
emulator, not the machine's serial port, and the two should not be confused.
