# `software/stardew/reference/`

Design assets for this component that are not written by this project: datasheets, manuals, articles, artwork, disk images. **Empty so far.** PDFs, scans, images and archives here are `.gitignore`d (the root `.gitignore`'s `**/reference/**` patterns); list each expected file below with what cites it, so a fresh clone can be refilled deliberately.

| File | What | Cited by |
|---|---|---|
