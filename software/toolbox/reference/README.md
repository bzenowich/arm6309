# `software/toolbox/reference/`

| File | What | Tracked |
|---|---|---|
| `parrots-image.jpg` | the photograph the toolbox's document page shows (`tools/mktbox.py` `PHOTO`) | ✗ — supplied, and kept out of history (git 790de82) |

Without it `mktbox.py` falls back to `software/tools/mkparrots.py`'s drawn illustration.
